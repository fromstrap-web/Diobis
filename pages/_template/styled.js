import styled from 'styled-components'
import tw from 'twin.macro'

export const Section = styled.div`
  ${tw`w-full h-full`}
`

export const Error = styled.div`
  ${tw`w-full h-full flex items-center justify-center bg-red-100`}
`
